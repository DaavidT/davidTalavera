# This file serves as an example for the .env file.
# Copy this file to .env and fill in your actual values.
# IMPORTANT: Never commit your .env file to version control if it contains sensitive data.

# -----------------------------------------------------------------------------
# General Site Configuration
# -----------------------------------------------------------------------------
# Example: PUBLIC_SITE_URL="https://yourdomain.com"
PUBLIC_SITE_URL="https://yourdomain.com"

# -----------------------------------------------------------------------------
# Blog & Project Features
# -----------------------------------------------------------------------------
# URL for your Google Calendar appointment scheduling page (if used)
PUBLIC_GOOGLE_CALENDAR_URL=""

# -----------------------------------------------------------------------------
# Contact Form (Resend Integration)
# -----------------------------------------------------------------------------
# Your Resend API key for sending emails from the contact form
# Get it from https://resend.com/
RESEND_API_KEY=""

# The email address where contact form submissions will be sent
CONTACT_FORM_TO_EMAIL="your-email@example.com"

# The "From" email address for emails sent via Resend.
# This usually needs to be a verified domain on Resend.
# Example: "noreply@yourdomain.com"
RESEND_FROM_EMAIL=""

# -----------------------------------------------------------------------------
# Contact Form Abuse Protection
# -----------------------------------------------------------------------------
# Per-IP limit: max submissions allowed inside the time window
CONTACT_RATE_LIMIT_MAX_REQUESTS="5"
# Per-IP window size in milliseconds (example: 1 hour)
CONTACT_RATE_LIMIT_WINDOW_MS="3600000"

# Global limit for all contact submissions in the same window
CONTACT_GLOBAL_RATE_LIMIT_MAX_REQUESTS="30"
# Global window size in milliseconds
CONTACT_GLOBAL_RATE_LIMIT_WINDOW_MS="3600000"
# Optional: comma-separated allowed origins for contact POST requests.
# Useful on Vercel when using multiple domains (apex, www, preview).
# Example:
# CONTACT_ALLOWED_ORIGINS="https://yourdomain.com,https://www.yourdomain.com"
CONTACT_ALLOWED_ORIGINS=""

# Minimum time (ms) expected before submit to help block basic bots
CONTACT_MIN_FORM_FILL_MS="2500"

# Maximum accepted payload size in bytes
CONTACT_MAX_CONTENT_LENGTH="10000"

# -----------------------------------------------------------------------------
# Other API Keys or Service Configurations (add as needed)
# -----------------------------------------------------------------------------
# Example: PUBLIC_ANALYTICS_ID="G-XXXXXXXXXX"
